module {{.ModulePath}}

go {{.GoVersion}}

// FS1: a pocket core requires only SDK and the shared pockets contract. Persistence lives in
// the stores/{turso,pgx} sibling modules; views (if any) in a views/ sibling.
require (
	github.com/gopernicus/gopernicus/pockets v0.1.0
	github.com/gopernicus/gopernicus/sdk v0.9.0
)
