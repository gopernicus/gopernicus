module {{.ModulePath}}

go 1.26.1

require github.com/gopernicus/gopernicus/sdk v0.1.0
{{- if .HasDB}}

require {{.ConnectorPath}} v0.1.0
{{- if eq .DB "turso"}}

require github.com/tursodatabase/libsql-client-go v0.0.0-20260528064733-9d5d30a29a60 // indirect
{{- end}}
{{- end}}

// Developing against a local gopernicus checkout instead of the tagged
// releases? Point a module at it with a replace directive (the gopernicus
// repo's scaffold-compile test injects these automatically; a human adds them
// by hand — see README.md "Local framework development"):
//
//   replace github.com/gopernicus/gopernicus/sdk => /abs/path/to/gopernicus/sdk
{{- if .HasDB}}
//   replace {{.ConnectorPath}} => /abs/path/to/gopernicus/{{.ConnectorRel}}
{{- end}}
