# Auth v3 delivery-runtime follow-up task board

Status only. Phase files contain authority, acceptance criteria, verification,
and execution logs. This track runs after `AV3-9.6` and before `AV3-9.7`.

Suggested implementer prompt:

> Execute task `AV3D-x.y` from
> `.claude/plans/authv3-delivery-refactor/<phase-file>.md`. Read
> `00-overview.md`, the full phase file, `.claude/agents/implementer.md`,
> `ARCHITECTURE.md`, and every cited module contract first. Preserve the current
> dirty worktree, implement only that task, run its exact verification, append
> the execution log, and update this board.

## Phase 0 — decisions and characterization

- [x] AV3D-0.1 — ratify delivery modes and construction matrix
- [x] AV3D-0.2 — characterize existing delivery security behavior
- [x] AV3D-0.3 — freeze generic jobs extension vocabulary

## Phase 1 — generic jobs hardening

- [x] AV3D-1.1 — lease-fenced kernel and queue transitions
- [x] AV3D-1.2 — logical-key admission and atomic supersession
- [x] AV3D-1.3 — claimed-payload checkpoint
- [x] AV3D-1.4 — retry policy, terminal callback, and bounded purge
- [x] AV3D-1.5 — memory/pgx/turso conformance and live race proof (hermetic + integration-vet green; live pgx/turso runs remain an open owner gate — env unset)

## Phase 2 — authentication delivery processor

- [x] AV3D-2.1 — versioned command and processor contract
- [x] AV3D-2.2 — move initialization and delivery policy into processor
- [x] AV3D-2.3 — dispatcher and secret-free status seams
- [x] AV3D-2.4 — migrate every outbound producer
- [x] AV3D-2.5 — optional lifecycle observer/events adapter (phase 2 gate green: both characterization harnesses + observer-variant parity under -race, producer tripwire, make check, make guard)

## Phase 3 — durable generic-jobs mode

- [x] AV3D-3.1 — composition adapter and host wiring (host-local `examples/auth-cms/internal/authjobs`; jobs fenced Service surface + FencedRuntime; auth DeliveryDispatcher/DeliveryJobRuntime seam; construction-order + Register-starts-nothing proven; auth-cms runs jobs mode end-to-end on the in-memory fenced queue; make check + make guard green)
- [x] AV3D-3.2 — encrypted admission and checkpointed initialization (host-level proofs over the real jobs-mode composition against an inspectable in-memory fenced queue; every persisted payload sealed incl. the opaque resolution input; restart-after-opaque-admission / after-checkpoint / after-provider-acceptance all resend the SAME secret; no feature code changed — 3.1's mechanism already conformant; make guard green; live pgx/turso restart proof remains AV3D-3.5)
- [x] AV3D-3.3 — duplicate, resend, and stale-worker behavior (host-level proofs over the real jobs-mode composition: receipt key = PII-free logical key ≠ execution id, status resolves latest-by-key; submit-once coalesces onto one active execution; Replace mints a fresh generation and status selects it; four adversarial-replacement stages — pending/initializing/checkpointed/sending — prove the stale handler cannot checkpoint or record success/failure after supersession (conflict), the fresh generation proceeds, and status = latest; in-flight provider race documented on the auth DeliveryDispatcher/Replace surface; `-race -count=5` deterministic; make guard green; live pgx/turso resend/stale-claim proof remains AV3D-3.5)
- [x] AV3D-3.4 — retry, terminal cleanup, lifecycle, and retention (error-aware `workers.WithFencedRetryDecider` kernel seam; jobs `Permanent` disposition + `Service.PurgeTerminal` + `ErrProcessTimeoutExceedsLease` construction validation; auth engine result → runtime retry/fail path via `DeliveryErrorPermanent` classifier through the host adapter — transient → capped exponential retry-at bounded then dead-letter, permanent → immediate dead-letter, parent cancellation → reclaimable; optional `EventObserver` wired into the jobs-mode transport emitting retried/dead_lettered/purged with `SafeObserve` containment; discard runs only after the recorded dead-letter, idempotent, failure never resurrects; host-driven bounded `authjobs.PurgeTerminal`; retrying→pending / dead-lettered→failed project through the receipt flow; `-race -count=3` deterministic; make check + make guard green; live pgx/turso proof remains AV3D-3.5)
- [x] AV3D-3.5 — production/live-store/restart/real-interaction proof (hermetic construction negatives extended: dispatcher-path encrypter/ack, dev-only-transport-in-production, composed ProcessTimeout≥LeaseFor → ErrProcessTimeoutExceedsLease; per-dialect LIVE run-and-look harness — `//go:build livedelivery`, turso also `integration`, workspace-resolved, hermetic build untouched — covering known/unknown parity, provider-timeout+retry off request path, restart at each checkpoint boundary, resend→latest generation, status/events no-secrets, terminal cleanup+purge; phase-3 gate green under -race (jobs/auth/auth-cms), `make check` + `make guard` green, migration filename parity both trees; REAL interaction drove register/verify/forgot-password/passwordless over live HTTP on the running auth-cms app with the generic-jobs FencedRuntime — verification/reset/sign-in-link all delivered OFF the request path, enumeration-safe skip observed pre-verification; LIVE pgx/turso runs remain the open owner gate — env unset, both dialect tests loud-SKIP and compile)

## Phase 4 — bounded in-process mode

- [x] AV3D-4.1 — fixed pool and bounded admission (bounded in-process runtime: `delivery.InProcessQueue` Dispatcher + `delivery.InProcessRuntime` fixed pool sharing a finite buffer — breaks the admission-before-authService cycle with no late-binding; typed `ErrDeliveryCapacity`/`ErrDeliveryClosed` (wrap `sdk.ErrConflict`) under a bounded admission deadline, never a silent drop; host-owned `Service.RunDelivery(ctx)` with cancellation propagation into in-flight `Handle` + bounded shutdown drain; `Register` starts no goroutine, proven both layers; in_process now builds the queue and requires the encrypter (fail-closed); defaults workers 2/capacity 256/admission 250ms/shutdown 15s; MEASURED peak concurrency == configured N (3, and 4 over 40 items — never one-per-request), goroutine delta = N+1; all proofs under `-race` (`-count=5` deterministic); submit-once/replace/status arbiter deferred to 4.2, checkpoint/retry/terminal to 4.3; `go build`/`go test -race`/`go vet` + `make guard` green)
- [x] AV3D-4.2 — process-local idempotency, replacement, and status retention (one-lock arbiter on `InProcessQueue`: logical-key + generation submit-once coalescing, `Replace` supersession fencing queued AND in-flight (`ErrDeliverySuperseded`), deterministic latest-by-key status; bounded retention — max entries + TTL, proven never to grow (200 keys → ≤8) and TTL-evict to unknown; admission holds the lock only for the generational decision, never the bounded send; ephemeral docs on the in_process surface + `RunDelivery`; all proofs under `-race` (`-count=5` deterministic incl. concurrent submit/replace one-winner); `go build`/`go test -race`/`go vet` + `make guard` green; checkpoint-persistence/retry deferred to 4.3, retention-knob validation to 4.5)
- [x] AV3D-4.3 — checkpoint, retry, timeout, and terminal cleanup (the bounded pool now runs the SAME `command.Engine` processor jobs mode runs, so it applies the identical provider timeout, error classification, attempt cap, capped context-cancellable backoff, observer transitions, and terminal challenge discard: `process` maintains an in-memory rendered-payload checkpoint — the fenced checkpoint closure records the sealed bytes so a process-local retry reuses the byte-identical secret (real-processor proof: opaque work initialized once, both sends identical, carry the checkpointed secret); a transient failure retries after a bounded `backoffWait` (occupies the worker slot — the pool reschedules nothing; single timer, no busy-loop, no per-retry goroutine) up to `MaxAttempts`, then `deadLetter` records the terminal and ONLY THEN `Discard`s (proven discard observes the recorded `dead_letter`, exactly once); a permanent failure dead-letters on attempt 1; a generation superseded before OR during backoff re-checks `current` and stops — no resend, no transition; shutdown cancels a parked backoff promptly (well under a 10s backoff, only attempt 1 ran); each provider send bounded by the engine timeout (hanging provider → capped retries → prompt dead-letter); observer parity via the reused processor — nil/erroring/panicking observer changes no outcome; NO claim lease across restart, no durability implied; `go build`/`go test -race`/`go vet` + `-race -count=5 TestInProcess` + `make guard` green; deliverychar.Run deferred to 4.5 — its crash-replay/lease + purge-retention cases model durable reclaim the ephemeral in_process mode deliberately lacks)
- [x] AV3D-4.4 — enumeration and saturation proof (in_process capacity/closed/shutdown outcomes proven IDENTICAL for known and unknown identifiers — same typed error class AND structural no-lookup-before-admission via resolver-call counters == 0 — against the REAL InProcessQueue/Runtime through a real authsvc.Service, for both forgot-password and passwordless; HTTP mapping ALIGNED to an honest 503 for a dropped bounded-delivery admission — never a 202-accepted lie — across forgot-password + passwordless JSON and form transports via one kind-based `deliveryUnavailable` helper, preserving known/unknown parity, was 500/409; account-resolved contracts proven — identifier-change PROOF surfaces the dispatch failure to the caller, identifier-change NOTICE commits then records a PII-free WARN without rollback or false-fail; all proofs under `-race`; `go build`/`go test -race`/`go vet` + `make guard` green; real mail/notifier + leak proof remains AV3D-4.5)
- [x] AV3D-4.5 — construction and real-interaction proof (in-process tuning knobs exposed on `auth.Config.InProcessDelivery` — nil-safe (zero → default), fail-closed: every negative bound + a status retention below capacity FAIL AT CONSTRUCTION with typed `delivery.ErrInProcess*Invalid`/`TooSmall` errors (wrap `sdk.ErrInvalidInput`), validated via `InProcessQueueConfig.Validate`/`InProcessRuntimeConfig.Validate` + self-validation in `NewInProcessRuntime`; production still requires the ephemeral acknowledgment; matrix extended with 8 negative + 1 tuned-positive case; multi-instance WARNING added to the `InProcessDeliveryConfig`/`DeliveryModeInProcess`/`RunDelivery` docs — each process keeps its own queue/de-duplication/status so two instances can each send; deliverychar.Harness wired against the REAL `InProcessQueue` arbiter + `InProcessRuntime.process` loop over the SAME `command.Engine` — 9/12 cases PASS, 3 honestly skipped with in-test justification + the dedicated test proving the property (crash-replay + purge-retention = durable-only; provider-timeout = reschedule-to-pending), never a silent drop; REAL in_process host over HTTP (test-scoped buildAuthConfig flipped to in_process): normal delivery observed on the recording mailer, saturation 503 over real HTTP (capacity 1, never a 202-after-drop), transient retry resends the byte-identical secret, cancellation + bounded shutdown drain; goroutine/leak proof — CONFIGURED workers=3, MEASURED baseline=2/during=6/delta=4 (3 workers + 1 supervisor) over 12 items (never one-per-request), returns to baseline after shutdown; phase 4 gate — `go test -race` (auth + bounded-runtime, `-count=5` deterministic), real-host suite under `-race -count`, `make check` (all checks passed), `make guard` (13 green))

## Phase 5 — migration and closeout handoff

- [x] AV3D-5.1 — remove bespoke delivery persistence and worker (deleted the private claim/poll/purge worker + worker-based harness, the bespoke deliveryjob domain + pgx/turso `delivery_jobs` stores + `0014_delivery_jobs.sql` in both trees, `Repositories.DeliveryJobs`/`RunDeliveryWorker`/`repoDispatcher` bridge/obsolete errors; `delivery.Service` Dispatcher now required; auth-cms residual DeliveryJobs wiring removed, storetest section dropped, conformance truncation lists + feature/host README rewritten to the two-mode model; ADDED Makefile G14 `guard-auth-no-delivery-repo` + G15 `guard-auth-no-request-time-provider` — both proven to fail on injection, `make guard` now fifteen; migration filename parity IDENTICAL 0001–0013 both trees; auth + pgx + turso (incl. integration vet) + auth-cms all green under `go build`/`go test -race`/`go vet`, `make check` + `make guard` PASS; live pgx/turso env unset → conformance loud-SKIP, remains the open owner gate)
- [x] AV3D-5.2 — canonical migrations and adopter upgrade (verified parity IDENTICAL both features both trees — auth 0001–0013, jobs 0001–0003; `git tag -l` EMPTY so the greenfield rewrite carries no append-only constraint; wrote the host-side **Auth delivery-runtime upgrade runbook** in `RELEASING.md` — stop workers/quiesce → drain [A] OR re-enqueue opaque ciphertext WITHOUT decrypting [B, pgx+turso INSERT…SELECT, logical_key preserved] → apply generic jobs schema + wiring → verify no active rows (exact SQL) → host-owned DROP TABLE → start jobs/`in_process` runtime; cross-linked from the auth README migrations section + v2→v3 note, avoiding the G14-forbidden `delivery_jobs` token; `make check` + `make guard` PASS)
- [x] AV3D-5.3 — proof-host variants and operational health (auth-cms already runs the recommended DURABLE jobs composition — verified + README made exemplary; added the small/development EPHEMERAL bounded-mode variant via a `DELIVERY_MODE` env switch in the existing main — smallest honest shape, `in_process` selects `authSvc.RunDelivery` behind a LOUD startup WARN stating crash-loss + per-process duplicate semantics, any other value fails safe to jobs, never silently ephemeral; exposed a host-COMPOSED, secret-free, bounded `GET /healthz/delivery` — new `internal/deliveryhealth` package — distinguishing runtime not-started/running (host lifecycle), backlog/saturation (jobs: admitted−terminal in-flight over the wrapped dispatcher; in_process: live queued/capacity/saturated from a small additive `auth.Service.InProcessQueueDepth` read seam), provider retry+dead-letter activity + observer failure (from the wrapped secret-free `authentication.delivery.*` events; `observer_failures` on emitter error); counters/gauges/enums only — no recipient/payload/logical-key; no-leak test proves the serialized output carries no canary; real-interaction tests drive in_process over HTTP+pool for all four states; `Register` starts no goroutine, no feature→feature import; auth + auth-cms green under `go build`/`go test -race`/`go vet`, `make check` + `make guard` PASS)
- [x] AV3D-5.4 — public docs and compatibility inventory (rewrote auth/jobs/events/auth-cms/RELEASING docs to the post-AV3D two-mode model — mode table + guarantees, jobs-mode composition-adapter + in_process `RunDelivery` snippets, events-observe-never-queue, at-least-once + resend/in-flight race, single-key rotation honesty, status retention/purge both modes, bounded-mode crash/multi-instance limits, and public removals/renames + adopter steps incl. `DeliveryStatus.Attempt`=0; documented the jobs fenced surface `FencedQueue`/`FencedRuntime`/primitives/`0003_fenced_job_queue`; appended the AV3D delivery-refactor delta to the AV3-9.4 inventory in NOTES.md; RESOLVED the RELEASING.md historical-DDL scope gap by preserve-and-annotate — validated v2→v3 `delivery_jobs` CREATE kept, actively-wrong forward prose (production checklist `RunDeliveryWorker`, tag table `0001…0014`/delivery-port, delivery-key rotation) corrected; compatibility grep inventory = ZERO active-code hits for every removed symbol/table (only 3 flagged stale comments + intentional `.claude`/RELEASING history), recorded in the phase log; caught + fixed a G14-forbidden `delivery_jobs` token accidentally introduced in the auth README; `make check` + `make guard` PASS)
- [x] AV3D-5.5 — final implementation-complete gate and AV3-9.7 handoff (ran the full gate FRESH `-count=1`: sdk/workers + jobs + authentication + auth/jobs pgx/turso stores + auth-cms + jobs-minimal all green under `-race` where supported, every live conformance + `livedelivery` pgx/turso leg LOUD-SKIPS verbatim and COMPILES — the standing open owner gate; protocols named to their carrying auth-cms tests — restart/stale-claim/resend/saturation/shutdown/real-provider-HTTP all PASS; migration parity IDENTICAL both features both trees; upgrade rehearsal dry-walked the RELEASING runbook against the real `fenced_job_queue`/`delivery_jobs` schema + confirmed every named symbol exists; `make check` + `make guard` PASS (fifteen guards); six adversarial searches all CLEAN — no plaintext secret in durable paths, no direct provider send outside `delivery/`, no bespoke auth job persistence, every production goroutine pool/lifecycle-bounded, bounded channels + finite max-entry+TTL retention map, sole delivery `Emit` is the best-effort observer never load-bearing; retired the three flagged stale closeout comments comment-only, touched modules re-ran green; appended milestone evidence + open-owner-gate sweep to `00-overview.md` and this phase log; `AV3-9.7` UNBLOCKED — not started per scope)
