//go:build integration

// Code generated by gopernicus. DO NOT EDIT.
// This file is regenerated every time 'gopernicus generate' runs.

package serviceaccountspgx

import (
	"context"
	"testing"

	"github.com/stretchr/testify/assert"
	"github.com/stretchr/testify/require"

	"github.com/gopernicus/gopernicus/core/repositories/auth/serviceaccounts"

	fixtures "github.com/gopernicus/gopernicus/core/testing/fixtures"

	"github.com/gopernicus/gopernicus/infrastructure/testing/pgxfixtures"
	"github.com/gopernicus/gopernicus/infrastructure/testing/testpgx"
	"github.com/gopernicus/gopernicus/sdk/fop"
	"github.com/gopernicus/gopernicus/sdk/logger"
)

// Ensure imports are used.
var (
	_ = assert.Equal
)

// setupTestStore creates a test database and store for integration tests.
// migrateTestDB must be defined in store_test.go (bootstrap file).
func setupTestStore(t *testing.T) (context.Context, *testpgx.TestPGX, *Store) {
	t.Helper()

	if testing.Short() {
		t.Skip("skipping integration test")
	}

	ctx := context.Background()
	db := testpgx.SetupTestPGX(t, ctx, testpgx.WithMigrations(migrateTestDB))
	store := NewStore(logger.NewNoop(), db.Pool)
	return ctx, db, store
}

func TestGeneratedServiceAccountStore_Get(t *testing.T) {
	ctx, db, store := setupTestStore(t)
	pgxfixtures.TruncatePublicSchema(t, ctx, db.Pool)

	created := fixtures.CreateTestServiceAccount(t, ctx, db)

	t.Run("found", func(t *testing.T) {
		result, err := store.Get(ctx, created.ServiceAccountID)
		require.NoError(t, err)
		assert.Equal(t, created.ServiceAccountID, result.ServiceAccountID)
	})

	t.Run("not found", func(t *testing.T) {
		_, err := store.Get(ctx, "nonexistent-id")
		require.Error(t, err)
	})
}

func TestGeneratedServiceAccountStore_List(t *testing.T) {
	ctx, db, store := setupTestStore(t)
	pgxfixtures.TruncatePublicSchema(t, ctx, db.Pool)

	// Create multiple records.
	const numRecords = 3
	for i := 0; i < numRecords; i++ {
		fixtures.CreateTestServiceAccount(t, ctx, db)
	}

	t.Run("returns all records", func(t *testing.T) {
		filter := serviceaccounts.FilterList{}
		orderBy := fop.NewOrder(serviceaccounts.DefaultOrderBy, serviceaccounts.DefaultOrderDirection)
		results, err := store.List(ctx, filter, orderBy, fop.PageStringCursor{}, false)
		require.NoError(t, err)
		assert.GreaterOrEqual(t, len(results), numRecords)
	})

	t.Run("respects limit", func(t *testing.T) {
		filter := serviceaccounts.FilterList{}
		orderBy := fop.NewOrder(serviceaccounts.DefaultOrderBy, serviceaccounts.DefaultOrderDirection)
		page := fop.PageStringCursor{Limit: 2}
		results, err := store.List(ctx, filter, orderBy, page, false)
		require.NoError(t, err)
		assert.LessOrEqual(t, len(results), 2)
	})

	t.Run("no duplicates", func(t *testing.T) {
		filter := serviceaccounts.FilterList{}
		orderBy := fop.NewOrder(serviceaccounts.DefaultOrderBy, serviceaccounts.DefaultOrderDirection)
		results, err := store.List(ctx, filter, orderBy, fop.PageStringCursor{}, false)
		require.NoError(t, err)

		seen := make(map[string]bool)
		for _, r := range results {
			assert.False(t, seen[r.ServiceAccountID], "duplicate record: %s", r.ServiceAccountID)
			seen[r.ServiceAccountID] = true
		}
	})
}

func TestGeneratedServiceAccountStore_Delete(t *testing.T) {
	ctx, db, store := setupTestStore(t)
	pgxfixtures.TruncatePublicSchema(t, ctx, db.Pool)

	created := fixtures.CreateTestServiceAccount(t, ctx, db)

	err := store.Delete(ctx, created.ServiceAccountID)
	require.NoError(t, err)

	// Verify record is gone.
	_, err = store.Get(ctx, created.ServiceAccountID)
	require.Error(t, err)
}

func TestGeneratedServiceAccountStore_SoftDelete(t *testing.T) {
	ctx, db, store := setupTestStore(t)
	pgxfixtures.TruncatePublicSchema(t, ctx, db.Pool)

	created := fixtures.CreateTestServiceAccount(t, ctx, db)

	err := store.SoftDelete(ctx, created.ServiceAccountID)
	require.NoError(t, err)

	// Verify record state changed.
	result, err := store.Get(ctx, created.ServiceAccountID)
	require.NoError(t, err)
	assert.Equal(t, "deleted", result.RecordState)
}
